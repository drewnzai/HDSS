package com.andrew.hdss.api;import com.andrew.hdss.api.swagger.SyncApi;import com.andrew.hdss.dtos.sync.SyncPullResponse;import com.andrew.hdss.dtos.sync.SyncPushRequest;import com.andrew.hdss.dtos.sync.SyncPushResponse;import com.andrew.hdss.services.SyncService;import io.swagger.v3.oas.annotations.tags.Tag;import lombok.RequiredArgsConstructor;import org.springframework.web.bind.annotation.*;import java.time.Instant;@RestController@RequestMapping("/api/sync")@RequiredArgsConstructor@Tag(name = "Entity Upload and Download")public class SyncController implements SyncApi {    private final SyncService syncService;    @PostMapping("/push")    public SyncPushResponse push(@RequestBody SyncPushRequest request) {        return syncService.push(request);    }    @GetMapping("/pull")    public SyncPullResponse pull(            @RequestParam Long locationId,            @RequestParam(required = false) Instant since    ) {        Instant effectiveSince = since != null ? since : Instant.EPOCH; // full download on first sync        return syncService.pull(locationId, effectiveSince);    }}