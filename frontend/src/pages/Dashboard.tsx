function Dashboard() {
    return (
        <div className="card">
            <h2>Dashboard</h2>
            <p>Records will surface here once a data endpoint is wired in.</p>
        </div>
    );
}

export default Dashboard;